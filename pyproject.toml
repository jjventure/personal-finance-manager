[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "personal-finance-manager"
version = "0.1.0"
description = "A personal finance manager built with Python"
requires-python = ">=3.12"

[tool.setuptools.packages.find]
where = ["src"]